<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="homeAsUpIndicator">@drawable/navicon</item>
        <item name="android:homeAsUpIndicator">@drawable/navicon</item>
        <item name="android:colorPrimary">@color/Pink</item>
        <!--   darker variant for the status bar and contextual app bars -->

        <item name="android:colorPrimaryDark">@color/Pink
        </item>
        <item name="android:navigationBarColor">@color/Pink</item>
    </style>

</resources>
